// todo: make sum of squares : 1^2 + 2^2 + 3^2 + ... + 1000^n (or crypto calculations)

export function hardWork(val) {
  let counter;
  for (let i = 0; i < 2000000000; i++) {
    counter++;
  }
  return val + 1;
}
